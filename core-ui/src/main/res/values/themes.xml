<resources xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SonicFlow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SonicFlow" parent="Base.Theme.SonicFlow" />

    <style name="PlayerButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:tint">@color/onSurface</item>
        <item name="android:background">@drawable/button_color_selector</item>
    </style>

    <style name="PlayerSongTitleStyle">
        <item name="android:textAppearance">@style/TextAppearance.Material3.HeadlineSmall</item>
        <item name="android:textColor">@color/onSurface</item>
        <item name="android:textStyle">bold</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:maxLines">1</item>
        <item name="android:gravity">start</item>
    </style>

    <style name="PlayerArtistNameStyle">
        <item name="android:textAppearance">@style/TextAppearance.Material3.TitleMedium</item>
        <item name="android:textColor">@color/onSurfaceVariant</item>
        <item name="android:maxLines">1</item>
        <item name="android:gravity">start</item>
    </style>

    <style name="PlayerAlbumNameStyle">
        <item name="android:textAppearance">@style/TextAppearance.Material3.TitleSmall</item>
        <item name="android:textColor">@color/secondary</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:gravity">start</item>
    </style>


</resources>